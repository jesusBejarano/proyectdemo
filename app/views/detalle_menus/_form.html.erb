<%= simple_form_for(@detalle_menu) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :DetallePedido %>
    <%= f.association :plato %>
    <%= f.association :TipoMenu %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

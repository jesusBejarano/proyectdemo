<%= simple_form_for(@ingrediente_plato) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :plato %>
    <%= f.association :ingrediente %>
    <%= f.input :cantidad %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
